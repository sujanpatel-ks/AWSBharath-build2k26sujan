# AgroCare AI — Development + test dependencies (NOT deployed to Lambda)

# Testing
pytest==8.3.4
pytest-cov==6.0.0
pytest-mock==3.14.0
moto[s3,dynamodb,ssm,bedrock]==5.0.25

# Linting / formatting
ruff==0.9.2

# Type checking
mypy==1.14.1
boto3-stubs[s3,dynamodb,ssm,bedrock-runtime,bedrock-agent-runtime]==1.35.99

# Runtime deps (same as layer — needed for local test runs)
boto3==1.35.99
botocore==1.35.99
pydantic==2.10.6
pydantic-core==2.27.2
typing-extensions==4.12.2
annotated-types==0.7.0
requests==2.32.3
